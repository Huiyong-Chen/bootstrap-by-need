{
  "name": "bootstrap-by-need",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm clean && tsc && cp src/utils/docx.js dist/utils/",
    "build:dev": "pnpm build && tsc -w",
    "dev": "pnpm build:dev & live-server .",
    "serve": "live-server ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "five-server": "^0.4.5",
    "typescript": "^5.9.3"
  }
}
